## 1. Implementation

- [ ] 1.1 Add DB schema for `tenantDocuments` table in `src/db/schema.ts` and migrations.
- [ ] 1.2 Implement backend-like helpers (Dexie access) in `src/features/tenants/store` to add/list/delete documents.
- [ ] 1.3 Add UI components: `TenantDocumentsUploader.vue`, `TenantDocumentsList.vue`.
- [ ] 1.4 Add actions to tenant profile view to `Validate` or `Refuse` applicant with optional reason and attach document evidence.
- [ ] 1.5 Write unit tests for DB schema, store actions, and composables.

## 2. Specs

- [ ] 2.1 Add spec delta describing required behaviors and scenarios (attached in `specs/tenants/spec.md`).

## 3. Documentation

- [ ] 3.1 Update README and change logs to describe tenant document workflow.
